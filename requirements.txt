# On Ubuntu 16.04 install
# sudo apt-get install python3 python3-dev gcc libffi-dev libxml2-dev libxslt1-dev
Scrapy==2.6.2
pycodestyle==2.0.0
awesome-slugify==1.6.5

# Lastly install "self" in editable mode so videodata is an importable module from anywhere
-e .
